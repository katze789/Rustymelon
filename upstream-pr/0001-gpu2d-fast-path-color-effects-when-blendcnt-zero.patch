diff --git a/src/GPU2D_Soft.cpp b/src/GPU2D_Soft.cpp
index 5adc545..23281fb 100644
--- a/src/GPU2D_Soft.cpp
+++ b/src/GPU2D_Soft.cpp
@@ -346,14 +346,22 @@ void SoftRenderer2D::DrawScanline_BGOBJ(u32 line, u32* dst)
     }
 
     // color special effects
-    // can likely be optimized
 
-    for (int i = 0; i < 256; i++)
+    if (GPU2D.BlendCnt == 0)
+    {
+        // No color special effect is enabled, so ColorComposite() returns the
+        // source pixel unchanged for every pixel: the pass reduces to a copy.
+        memcpy(dst, BGOBJLine, 256 * sizeof(u32));
+    }
+    else
     {
-        u32 val1 = BGOBJLine[i];
-        u32 val2 = BGOBJLine[256+i];
+        for (int i = 0; i < 256; i++)
+        {
+            u32 val1 = BGOBJLine[i];
+            u32 val2 = BGOBJLine[256+i];
 
-        dst[i] = ColorComposite(i, val1, val2);
+            dst[i] = ColorComposite(i, val1, val2);
+        }
     }
 }
 
